services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: erkpratas
    ports:
      - "5432:5432"
    volumes:
      - erk_pg_data:/var/lib/postgresql/data

volumes:
  erk_pg_data:
